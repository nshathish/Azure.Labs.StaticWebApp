# Copilot Instructions

## Project Guidelines
- Avoid hard-coded deployment-specific URLs; configure environment-specific values (like allowed CORS origins) via settings.
- Check ApiBaseAddress configuration only in Development; in non-development use same-origin base address.